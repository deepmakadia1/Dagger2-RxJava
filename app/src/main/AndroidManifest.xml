<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dagger2_rxjava">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppClass"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.DetailRecipeActivity" />
        <activity android:name=".ui.activity.DrinksActivity" />
        <activity android:name=".ui.activity.FoodActivity" />
        <activity android:name=".ui.activity.CategoryListActivity" />
        <activity android:name=".ui.activity.DrinkListActivity" />
        <activity android:name=".ui.activity.DetailDrinksActivity"></activity>
    </application>

</manifest>